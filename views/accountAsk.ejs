<!DOCTYPE html>
<html>
<%-include('base')-%>

<!DOCTYPE html>
<html>
<%-include('base')-%>

<body class="d-flex flex-column min-vh-100 text-center">
<%# include('header')-%>

<div class="form-signin m-auto">
    <form method="post" action="/auth/signup">
        <div class="position-relative">
            <img src="../style/images/logo.png" class="mb-4" width="236,4" height="197,8" alt="Logo">
        </div>
        <h1 class="h3 mb-3 fw-normal">Demander un compte</h1>
        <% if (failed) {%>
            <p class="mb-3 fw-normal text-danger">Erreur lors de la demande de compte</p>
        <% } %>
        <div class="form-floating mb-3">
            <input type="text" class="form-control" id="email" name="email" placeholder="n.prenom@mfgs.fr" required autofocus>
            <label for="email">Adresse email pro</label>
        </div>
        <div class="form-check mb-3">
            <input type="checkbox" class="form-check-input" id="DSIO_status" name="DSIO_status">
            <label class="form-check-label" for="DSIO_status">Membre du service informatique</label>
        </div>
        <div class="form-check mb-3">
            <input type="checkbox" class="form-check-input" id="ADMIN_status" name="ADMIN_status">
            <label class="form-check-label" for="ADMIN_status">Droit d'administrateur du système</label>
        </div>
        <button class="btn btn-primary w-100 py-2 mb-3" type="submit">Envoyer la demande</button>
    </form>
</div>

</body>

<%-include('scripts')-%>
</html>